/**
 * A class that defines the behaviour of classes that implements this interface.
 */

public interface WeightInterface {
    void add(Object wt);
    boolean compare(Object wt);
    Object getWT();
    void resetWT();

}
